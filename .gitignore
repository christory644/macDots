.zshenv_secrets
config/zsh/.zsh_history
config/zsh/.zhistory
config/zsh/.zcompdump
.DS_STORE
.java-version
.claude/
result

# Private repo manifest (work repos, licensed content)
repos-private.yml

# Allow encrypted secrets and age config, but never plain keys
secrets/*
!secrets/*.age
!secrets/age-identity.txt
!secrets/age-recipient.txt
